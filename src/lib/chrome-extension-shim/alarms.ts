function create(name: string, alarmInfo: chrome.alarms.AlarmCreateInfo): void {
  window.setInterval()
}

const onAlarm = {
  addListener,
  removeListener,
}

export default {
  create,
  onAlarm,
}
